# 화면 규격

## 목적

모든 주요 화면이 같은 게임 셸을 쓴다는 것과, 그 셸 안에서 각 화면이 무엇을 어디에 두는지를 정한다. 화면이 바뀌어도 플레이어가 상단 상태와 우측 패널의 자리를 다시 배우지 않아야 한다.

이 문서는 화면의 구조와 시각 규약만 다룬다. 각 화면이 보여주는 규칙과 수치의 근거는 해당 시스템 문서에 있고, 플레이어가 무엇을 이해해야 하는지는 [온보딩과 인터페이스](ONBOARDING_AND_INTERFACE.md)에 있다.

## 게임 셸

```text
GameShell
├─ TopStatusBar   길잡이 등급 · 명성 · 골드 · 승급 가능 여부 · 남은 던전
├─ MainContent    좌측 약 60%
└─ RightPanel     우측 약 40%
```

좌우 비율은 3:2(좌 60%·우 40%)다. 왼쪽은 게임 세계와 플레이어가 조작하는 대상, 오른쪽은 그 대상의 상세와 상태다. 화면이 바뀔 때 셸은 유지하고 두 영역의 콘텐츠만 교체한다.

상단 상태 바는 탐험 중에도 사라지지 않는다. 던전 안에서는 던전 이름과 현재 위험도를 함께 표시한다. 현재 명성이 내려가도 한번 얻은 길잡이 등급은 유지되므로, 둘을 서로 다른 표시로 구분한다.

## 고정 캔버스

| 기준 | 값 |
| --- | --- |
| 고정 캔버스 | 1920×1080 (16:9) |
| 창 대응 | 캔버스를 균일 확대·축소하고 남는 공간은 레터박스 |

화면은 창 크기와 무관하게 늘 같은 1920×1080 그림이다. 창이 작아지면 배치를 다시 짜는 것이 아니라 그림 전체가 작아진다. 이것은 관리 프로그램이 아니라 게임이기 때문이다. 플레이어가 창을 줄였다고 해서 방금 보던 화면과 다른 화면을 만나면 안 된다.

창 비율이 16:9가 아니면 남는 공간은 검은 레터박스로 남고 캔버스는 가로·세로 모두 가운데 정렬된다. 어떤 창 크기에서도 가로·세로 스크롤은 만들지 않는다. 가로로 잘린 정보는 플레이어가 있는 줄도 모르기 때문이다.

## 화면별 구조

| 화면 | MainContent | RightPanel |
| --- | --- | --- |
| 인트로 | 길잡이 역할과 캠페인 목표 | 우측 레일 유지 · 콘텐츠 없음 |
| 게시판 | 게시판 위 공고 최대 5장 | 던전 정보 · 출전 3인 · 계약 |
| 지도 | 범례 없는 공간형 지도 | 파티 상태 · 다음 지점 · 이동 |
| 진행 | 상 40% 장면 슬롯 / 하 60% 플레이어 콘솔(행동·조언 / 진행 기록) | 실시간 파티 상태 · 최근 반응 |
| 정산·엔딩 | 원인 순서대로 나열한 정산 | 위험도·보상 변화 · 승급하기 |

### 인트로

우측 패널에 담을 캠페인 상태가 아직 없으므로 콘텐츠는 비워 둔다. 다만 공통 셸의 3:2(좌 60%·우 40%) 구조와 우측 레일은 유지하며, 인트로도 다른 화면과 같은 열 비율을 사용한다. 화면별 콘텐츠만 교체하고 셸의 두 열을 합치지 않는다. 상단 상태 바는 시작 값 그대로 유지해, 플레이어가 첫 공고를 고르기 전에 자신이 가진 것을 본다.

### 게시판

왼쪽은 실제 길드 게시판처럼 보이되 공고 하나하나는 클릭 가능한 카드다. 다섯 장을 반듯하게 정렬하지 않고 읽기 쉬운 수준의 비대칭을 둔다. 카드 크기는 모두 같고 위치와 미세한 회전만 다르게 한다.

공고에는 던전 이름, 위험도 ★1~5, 진입 가능 여부, 보상 요약만 적는다. 생태 추론 정보는 던전 진입 뒤 사건과 진행 기록에서 공개한다. 출전 인물과 생존 인원별 계약 조건은 우측 상세에서 본다.

우측 상세에는 선택한 던전의 이름·위험도·보상, 출전 3인의 직업·성격·HP·신뢰·소지 골드, `전원/2명/1명 생존`과 `전원 사망`의 계약 결과를 보여준다. 별도의 정찰 보고·답사 기록·소요 시간 영역은 두지 않는다.

진입 불가 공고도 숨기지 않는다. 붉은 테두리와 진입 불가 문구로 구분하고, 클릭하면 우측에서 사유를 확인할 수 있다. 캠페인이 끝나는 이유를 플레이어가 공고로 직접 본다.

### 지도

범례 영역은 두지 않는다. 지점 자체를 아이콘으로 표현하고(몬스터 · 모닥불 · `?` · 보스 전용 문양), 다음 다섯 가지를 지도만 보고 구분할 수 있게 한다.

- 현재 위치
- 방문 완료
- 선택 가능한 다음 지점
- 지금 선택할 수 없는 다른 갈래
- 보스방

선 위의 원형 노드가 아니라 방·복도·갈림길·합류 지점이 있는 공간으로 그린다. 우측 하단에는 현재 선택한 다음 지점과 이동 버튼을 둔다.

### 진행

왼쪽을 위아래로 나눈다. 위 40%는 파티가 움직이고 싸우는 장면 슬롯이고, 아래 60%는 플레이어 콘솔이다. 조언 본문과 기록이 넓은 자리를 필요로 해 비율을 조정하더라도, 플레이어가 읽고 조작하는 하단 영역이 장면보다 넓다는 원칙은 유지한다.

하단 콘솔은 다음 두 모드를 같은 자리에서 전환한다.

```text
[행동 / 조언] [진행 기록]
```

`행동 / 조언`에서는 현재 상황 묘사와 조언 3개를 보여준다. 상황 묘사가 추론의 근거를 실어 나르므로 조언보다 먼저 읽히도록 놓는다. 조언 3개는 항상 같은 디자인이며 무엇을 감추고 무엇을 보여주는지는 [정보와 기만](../systems/INFORMATION_AND_DECEPTION.md)의 「피드백 원칙」을 따른다.

`진행 기록`에서는 현재 원정에서 실제로 확인한 내용을 시간 순서로 다시 볼 수 있다. 최소 필터는 다음과 같다.

```text
[전체] [단서] [전투] [생태]
```

`생태`에서는 E2가 직접 공개한 **확인된 생태**와 원정 중 본 **관찰 단서**를 구분한다. 관찰 단서를 숨은 규칙 문장으로 자동 승격하거나 정답 처리하지 않는다.

새 선택이 필요하면 `행동 / 조언`을 우선 보여주고, 선택과 결과가 끝났거나 자동 전투가 진행 중이면 `진행 기록`을 우선 보여준다. 플레이어는 언제든 두 모드를 직접 전환할 수 있다.

선택 뒤에는 인과 순서로 보여준다.

```text
선택 → 파티원별 반응 → 사건 결과 → HP · 신뢰 · 상태 변화
```

조언의 내부 `help / harm / neutral`, 정합·모순 관계, 수용·적발 확률은 선택 전후 모두 화면에 직접 표시하지 않는다. 플레이어는 결과 문구와 실제 변화, 신뢰 변화 이유로 판단을 학습한다.

우측은 살아 있는 파티원의 HP·신뢰·소지 골드와 최근 반응을 실시간으로 갱신한다.

### 정산·엔딩

왼쪽은 숫자를 나열하지 않고 원인 순서로 나열한다. 오른쪽에는 던전 위험도와 보상이 어떻게 바뀌었는지, 그리고 `승급하기` 버튼을 둔다. 승급은 자동으로 일어나지 않으므로 버튼이 유일한 승급 경로다.

## 시각 방향

다크 판타지로 통일한다. 짙은 목재 프레임, 금속 장식, 양피지 질감, 따뜻한 광원, 금색 계열 테두리와 포인트다. 관리 프로그램이 아니라 길드와 던전 세계 안에 있는 물건처럼 보이게 한다.

세계관 표현과 조작 UI를 분리하지 않는다. 게시판 배경은 실제 게시판처럼, 공고 종이는 실제 클릭 가능한 카드처럼 만든다.

| 색 | 의미 |
| --- | --- |
| 초록 | 활성 · 선택 · 진행 가능 · 수용 |
| 금색 | 선택 가능 · 강조 · 의심 |
| 붉은색 | 위험 · 진입 불가 · 보스 · 적발 |

## 색 외 단서 원칙

세 시각 레퍼런스는 위 시각 언어와 정보 위계의 근거다. [캠페인 게시판](../diagram/png/REFERENCE_UI_01_CAMPAIGN_BOARD.png)은 좌 공고·우 계약 상세, [던전 지도](../diagram/png/REFERENCE_UI_02_DUNGEON_MAP.png)는 경로·파티 정보, [던전 진행](../diagram/png/REFERENCE_UI_03_DUNGEON_PROGRESS.png)은 장면·선택지·최근 반응 구획을 참고한다.

레퍼런스 PNG는 구현 자산이 아니다. React 화면에 임베드하거나 복제하지 않고, CSS의 명도·프레임·정보 밀도와 콘텐츠 구획에만 반영한다.
**색만으로 의미를 전달하지 않는다.** 색을 쓸 때는 항상 텍스트나 형태 단서를 함께 둔다.

| 상황 | 색 | 함께 두는 단서 |
| --- | --- | --- |
| 진입 불가 공고 | 붉은 테두리 | `진입 불가`와 사유 문구 |
| 지도의 현재 위치·방문 완료·비활성 | 초록 · 흐린 회색 · 어두움 | 아이콘 형태와 테두리 모양 |
| 보스방 | 붉은 문양 | 보스 전용 문양 |
| 파티원 반응 | 초록 · 금색 · 붉은색 | `수용` · `의심` · `적발` 문구 |

조언 유형은 색으로도 텍스트로도 표시하지 않는다. 이 원칙은 감춰야 할 것을 드러내라는 뜻이 아니라, 드러내기로 한 것을 색에만 맡기지 말라는 뜻이다.

## 구현 구조

```text
app/play/layout.tsx
└─ GameShell
   ├─ TopStatusBar
   ├─ MainContent
   └─ RightPanel
```

게임 도메인을 아는 컴포넌트는 `components/game/`에, 도메인을 모르는 범용 프레임과 패널은 `components/ui/`에 둔다.

모션은 공고 선택, 경로 활성화, 파티 이동, 피격, 조언 선택, 반응 표시, HP·신뢰 변화 같은 결과 피드백에 쓴다. 모션을 보여주려고 정보를 늦추거나 가리지 않는다.

## 프로토타입 범위 밖

- 자동 전투 장면의 실제 연출과 애니메이션. 진행 화면 상단 40%는 구조와 자리까지만 만든다
- 보스전 턴 기록의 시각 표현
- 최종 아트 스타일과 고유 던전·보스 서사 화면
- 작은 브라우저 창과 모바일 대응
- 지도 카메라 이동과 줌
- 저장·복원과 계정 UI

## 관련 문서

- [온보딩과 인터페이스](ONBOARDING_AND_INTERFACE.md)
- [게임 원칙](../GAME_PRINCIPLES.md)
- [핵심 게임 루프](../design/CORE_GAME_LOOP.md)
- [정보와 기만](../systems/INFORMATION_AND_DECEPTION.md)
- [던전 이벤트와 보스](../systems/DUNGEON_EVENTS_AND_BOSSES.md)
- [캠페인 개편 설계](../superpowers/specs/2026-08-19-lattebun-campaign-rework-design.md)
