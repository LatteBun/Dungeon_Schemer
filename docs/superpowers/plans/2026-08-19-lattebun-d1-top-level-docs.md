# D1 최상위·루프 문서 개정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 캠페인 개편의 최상위 문서 3개를 새 규칙으로 갈아끼우고, 폐기된 용어가 다시 스며들지 못하게 감시 테스트로 고정한다.

**Architecture:** 문서 개정을 TDD로 다룬다. 먼저 폐기 용어와 필수 앵커를 검사하는 테스트를 실패 상태로 만들고, 문서를 하나씩 고쳐 통과시킨다. 감시 테스트는 `정리 완료된 문서` 목록을 들고 있으며 `D2`~`D6`이 각자의 문서를 정리할 때 그 목록에 줄을 더한다. 개정 대상 3개는 서로 문장을 참조하므로 한 사람이 연속으로 작업한다.

**Tech Stack:** Markdown, TypeScript, Vitest 4

**Spec:** `docs/superpowers/specs/2026-08-19-lattebun-campaign-rework-design.md`

## Global Constraints

- 캐릭터 풀은 **30명**, 5직업 × 6명, 5성격 × 6명. 캠페인 도중 충원 없음
- 던전 **15개**, 테마 3종(거미굴 · 폐광 · 묘지)을 5개씩. 초기 위험도 ★1 3개 · ★2 4개 · ★3 4개 · ★4 3개 · ★5 1개
- 길잡이 등급의 진입 한계: C는 ★2, B는 ★3, A는 ★4, S는 ★5까지
- 시작 명성 **30**, 명성 하한 **0**, 시작 골드 10
- 승급은 자동이 아니다. 요구 명성 60 / 120 / 200 무료 승급, 또는 골드 150 / 320 / 600 지불 승급
- 정보 전달 기회는 현재 위험도 기준 ★1 2회 · ★2 3회 · ★3 4회 · ★4 5회 · ★5 5회
- 카드 유형은 선택 전에 어떤 방식으로도 표시하지 않는다
- 엔딩 6종: 원정 종료 · 불신의 대가 · 누적 고발 · 실직 · 인력 소진. `길잡이 자격 박탈`은 폐기
- 문서에 확정되지 않은 수치를 사실처럼 적지 않는다 (`docs/README.md` 문서 갱신 규칙)
- 한국어로 쓰고 기존 문서의 문체를 따른다. 같은 규칙을 여러 문서에 복사하지 않고 가장 직접적인 문서를 기준으로 연결한다

---

## File Structure

| 파일 | 책임 |
| --- | --- |
| `docs/DOCUMENT_TERMINOLOGY.test.ts` (신규) | 정리 완료된 공식 문서에 폐기 용어가 없고 필수 앵커가 있는지 감시 |
| `docs/GAME_PRINCIPLES.md` (수정) | 최상위 원칙과 프로토타입 고정 범위 |
| `docs/design/GAME_OVERVIEW.md` (수정) | 캠페인 목표, 핵심 재미, 엔딩 개요 |
| `docs/design/CORE_GAME_LOOP.md` (수정) | 캠페인 루프, 단계 목록, 자원 모델 |
| `docs/technical/CAMPAIGN_REWORK_WORK_ASSIGNMENT.md` (수정) | `D1` 상태 갱신 |

감시 테스트를 `docs/` 최상위에 두는 이유는 검사 대상이 특정 하위 폴더가 아니라 공식 문서 전체이기 때문이다. `docs/technical/`의 배정표 검사가 배정표 옆에 있는 것과 같은 원리다.

---

## Task 1: 용어 감시 테스트

**Files:**
- Create: `docs/DOCUMENT_TERMINOLOGY.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces: `RETIRED_TERMS`, `CLEANED_DOCS`, `REQUIRED_ANCHORS` 상수. `D2`~`D6`은 `CLEANED_DOCS`와 `REQUIRED_ANCHORS`에 자기 문서를 더한다

- [ ] **Step 1: 감시 테스트를 작성한다**

`docs/DOCUMENT_TERMINOLOGY.test.ts`:

```typescript
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { describe, expect, it } from "vitest";

/**
 * 공식 문서의 용어 감시.
 *
 * 캠페인 개편은 도메인 모델과 난이도 축을 동시에 바꾸므로 폐기된 용어가
 * 문서 곳곳에 남는다. 이전 개편에서 `docs/systems/`만 훑었다가
 * `docs/design/CORE_GAME_LOOP.md`의 옛 값을 놓친 적이 있다. 사람이 눈으로
 * 훑는 대신 여기서 고정한다.
 *
 * `CLEANED_DOCS`는 개정이 끝난 문서만 담는다. D2~D6이 자기 문서를 정리할
 * 때 이 목록에 줄을 더한다. 아직 정리하지 않은 문서를 미리 넣으면 이 검사가
 * 상시 빨간불이 되어 아무도 보지 않게 된다.
 *
 * 위반은 모아서 한 번에 보여준다. 루프 안에서 바로 단정하면 위반이 여럿일 때
 * 첫 번째만 드러나 여러 번 고쳐야 한다.
 */

const DOCS_ROOT = import.meta.dirname;

/** 개편으로 폐기한 표현. 정리된 문서에 남아 있으면 안 된다. */
const RETIRED_TERMS: readonly string[] = [
  "완성 파티",
  "예비 인원",
  "지속 파티",
  "자동 재편",
  "승급 점수",
  "던전 등급",
  "길잡이 자격 박탈",
  "C 6개",
  "C→B→A→S",
  "현재 명성 0",
  "음수가 될 수 있음",
  "C 2회",
];

/** 개정이 끝난 문서. D2~D6이 진행되면서 늘어난다. */
const CLEANED_DOCS: readonly string[] = [
  "GAME_PRINCIPLES.md",
  "design/GAME_OVERVIEW.md",
  "design/CORE_GAME_LOOP.md",
];

/**
 * 폐기 용어를 지우는 것만으로는 문장을 통째로 삭제해도 통과한다.
 * 새 규칙이 실제로 적혔는지 앵커로 확인한다.
 */
const REQUIRED_ANCHORS: Readonly<Record<string, readonly string[]>> = {
  "GAME_PRINCIPLES.md": ["캐릭터 30명", "위험도", "테마 3종"],
  "design/GAME_OVERVIEW.md": ["캐릭터 30명", "위험도"],
  "design/CORE_GAME_LOOP.md": ["월드턴", "위험도"],
};

function read(relativePath: string): string {
  // Windows에서 core.autocrlf가 CRLF로 체크아웃해도 검사가 동작하도록 통일한다.
  return readFileSync(join(DOCS_ROOT, relativePath), "utf8").replace(
    /\r\n/g,
    "\n",
  );
}

describe("공식 문서 용어", () => {
  it("정리된 문서에 폐기 용어가 없다", () => {
    const violations: string[] = [];

    for (const path of CLEANED_DOCS) {
      const text = read(path);
      for (const term of RETIRED_TERMS) {
        const count = text.split(term).length - 1;
        if (count > 0) {
          violations.push(`${path}: "${term}" ${count}건`);
        }
      }
    }

    expect(violations, "남아 있는 폐기 용어").toEqual([]);
  });

  it("정리된 문서에 새 규칙의 앵커가 있다", () => {
    const missing: string[] = [];

    for (const path of CLEANED_DOCS) {
      const text = read(path);
      for (const anchor of REQUIRED_ANCHORS[path] ?? []) {
        if (!text.includes(anchor)) {
          missing.push(`${path}: "${anchor}" 없음`);
        }
      }
    }

    expect(missing, "빠진 앵커").toEqual([]);
  });

  it("정리 목록의 모든 문서에 앵커가 정의돼 있다", () => {
    const undefinedAnchors = CLEANED_DOCS.filter(
      (path) => REQUIRED_ANCHORS[path] === undefined,
    );
    expect(undefinedAnchors, "앵커가 없는 정리 문서").toEqual([]);
  });
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `npx vitest run docs/DOCUMENT_TERMINOLOGY`

Expected: FAIL. 두 테스트가 깨지고 위반 목록이 정확히 아래와 같이 나온다.

```
남아 있는 폐기 용어
[
  'GAME_PRINCIPLES.md: "완성 파티" 1건',
  'GAME_PRINCIPLES.md: "예비 인원" 2건',
  'GAME_PRINCIPLES.md: "지속 파티" 1건',
  'GAME_PRINCIPLES.md: "자동 재편" 1건',
  'GAME_PRINCIPLES.md: "던전 등급" 1건',
  'GAME_PRINCIPLES.md: "C 6개" 1건',
  'design/GAME_OVERVIEW.md: "완성 파티" 1건',
  'design/GAME_OVERVIEW.md: "예비 인원" 1건',
  'design/GAME_OVERVIEW.md: "승급 점수" 2건',
  'design/GAME_OVERVIEW.md: "C 6개" 1건',
  'design/CORE_GAME_LOOP.md: "완성 파티" 2건',
  'design/CORE_GAME_LOOP.md: "예비 인원" 2건',
  'design/CORE_GAME_LOOP.md: "자동 재편" 1건',
  'design/CORE_GAME_LOOP.md: "승급 점수" 3건',
  'design/CORE_GAME_LOOP.md: "던전 등급" 1건',
  'design/CORE_GAME_LOOP.md: "C 6개" 1건',
  'design/CORE_GAME_LOOP.md: "C→B→A→S" 1건',
  'design/CORE_GAME_LOOP.md: "현재 명성 0" 1건',
  'design/CORE_GAME_LOOP.md: "음수가 될 수 있음" 1건',
  'design/CORE_GAME_LOOP.md: "C 2회" 1건'
]
```

세 번째 테스트(`앵커가 정의돼 있다`)는 통과한다.

건수가 위와 다르면 문서가 이미 손대진 상태다. 계속하기 전에 `git status`로 확인한다.

- [ ] **Step 3: 커밋**

```bash
git add docs/DOCUMENT_TERMINOLOGY.test.ts
git commit -m "테스트: 공식 문서 용어 감시를 실패 상태로 추가한다"
```

---

## Task 2: GAME_PRINCIPLES.md 개정

**Files:**
- Modify: `docs/GAME_PRINCIPLES.md`
- Test: `docs/DOCUMENT_TERMINOLOGY.test.ts` (Task 1에서 작성)

**Interfaces:**
- Consumes: Task 1의 감시 테스트
- Produces: 앵커 `캐릭터 30명` · `위험도` · `테마 3종`이 들어간 최상위 원칙 문서

- [ ] **Step 1: 원칙 2에 카드 비공개 조항을 넣는다**

`## 2. 정보는 용사 파티에게 전달한다` 절에서, `- 중립은 비교적 안전하지만 효과가 약하다.` 다음 줄과 `보스에게 파티 정보를 넘기거나` 문단 사이에 아래 문단을 넣는다.

```markdown
카드의 유형은 선택 전에 표시하지 않는다. 플레이어는 던전의 생태 규칙과 현장 단서로 진위를 추론한다.
```

- [ ] **Step 2: 원칙 3의 본문을 바꾼다**

찾을 문장:

```markdown
파티를 하나의 신뢰 수치로 취급하지 않는다. 각 파티원은 직업·성격·개인 신뢰·HP·소지 골드·행동 기억을 가지며 탐험이 끝나도 유지한다. 같은 정보에도 서로 다르게 반응해야 한다.
```

바꿀 문장:

```markdown
파티를 하나의 신뢰 수치로 취급하지 않는다. 캐릭터는 직업·성격·개인 신뢰·HP·소지 골드·행동 기억을 가지며 원정이 끝나도 유지한다. 출전 파티는 원정마다 새로 편성하지만 캐릭터와 그 관계는 캠페인 내내 이어진다. 같은 정보에도 서로 다르게 반응해야 한다.
```

- [ ] **Step 3: 원칙 4의 본문을 바꾼다**

찾을 문장:

```markdown
파티 생존은 명성, 계약금, 지속 가능한 인력과 관계를 준다. 전멸은 명성과 파티 자원을 잃는 대신 사망자의 골드를 얻는다. 어느 한쪽이 항상 정답이 되어서는 안 된다.
```

바꿀 문장:

```markdown
파티 생존은 명성, 계약금, 다음 원정에 쓸 인력과 관계를 준다. 전멸은 명성을 깎고 캐릭터를 영영 잃는 대신 사망자의 골드를 얻는다. 캐릭터는 캠페인 도중 충원되지 않으므로 배신의 대가는 누적된다. 어느 한쪽이 항상 정답이 되어서는 안 된다.
```

- [ ] **Step 4: 원칙 5의 본문을 바꾼다**

찾을 문장:

```markdown
HP와 신뢰, 생존자, 예비 인원, 남은 던전, 던전 등급, 현재 명성, 현재 골드, 누적 획득 골드, 길잡이 등급은 다음 공고와 엔딩에 연결된다. 한 탐험을 초기화해 이전 결과를 지우지 않는다.
```

바꿀 문장:

```markdown
HP와 신뢰, 생존자, 남은 캐릭터, 남은 던전, 던전 위험도, 신뢰 0 누적 인원, 현재 명성, 현재 골드, 길잡이 등급은 다음 공고와 엔딩에 연결된다. 한 원정을 초기화해 이전 결과를 지우지 않는다.
```

- [ ] **Step 5: 원칙 6의 본문을 바꾼다**

이 원칙과 카드 비공개는 겉보기에 충돌한다. 무엇을 감추고 무엇을 알려주는지 문서에서 직접 가른다.

찾을 문장:

```markdown
지도에는 전체 경로와 사건 제목·종류·대략적인 위험을 보여주고 정확한 결과와 난수는 숨긴다. 공고에는 지원 조건, 보상, 파티 상태와 파티원별 소지 골드를 보여준다. 선택 후에는 HP·신뢰·자원 변화의 원인을 설명한다.
```

바꿀 문장:

```markdown
지도에는 전체 경로와 보스 위치를 보여주고 정확한 피해·보상과 난수는 숨긴다. 공고에는 진입 조건, 위험도, 보상, 답사한 생태 규칙, 출전 캐릭터의 상태를 보여준다. 선택 후에는 HP·신뢰·자원 변화의 원인을 설명한다.

감추는 것과 알려주는 것을 가른다. 정보 카드의 진위와 난수 결과는 감춘다. 그러나 진위를 판단할 근거인 생태 규칙과 현장 단서는 감추지 않는다. 플레이어가 실패했을 때 놓친 단서를 짚을 수 있어야 하고, 알 방법이 없었다고 느껴서는 안 된다.
```

- [ ] **Step 6: 원칙 8의 본문을 바꾼다**

찾을 문장:

```markdown
던전, 파티, 게시판 연결, 지도, 카드, 반응, 자동 재편은 캠페인 시드로 재현할 수 있어야 한다. 콘텐츠 부족이나 불가능한 상태를 숨기기 위해 조용히 다시 뽑지 않는다.
```

바꿀 문장:

```markdown
던전, 캐릭터 풀, 임시 파티 편성, 게시판, 지도, 카드, 반응, 월드턴은 캠페인 시드로 재현할 수 있어야 한다. 콘텐츠 부족이나 불가능한 상태를 숨기기 위해 조용히 다시 뽑지 않는다.
```

- [ ] **Step 7: 원칙 9의 본문을 바꾼다**

찾을 문장:

```markdown
범위를 줄일 때는 고유 콘텐츠 양과 시각 연출을 줄인다. 개인 신뢰, 정보 선택, 지속 파티, 생존과 배신의 대가, 캠페인 정산은 제거하지 않는다.
```

바꿀 문장:

```markdown
범위를 줄일 때는 고유 콘텐츠 양과 시각 연출을 줄인다. 개인 신뢰, 정보 선택, 캐릭터의 지속, 추론의 근거, 생존과 배신의 대가, 캠페인 정산은 제거하지 않는다.
```

- [ ] **Step 8: 프로토타입 고정 범위를 통째로 바꾼다**

찾을 블록:

```markdown
- 던전 15개: C 6개, B 4개, A 3개, S 2개
- 출전 파티: 모든 등급에서 서로 다른 직업의 3인
- 초기 인력: 완성 파티 15팀과 예비 인원 6명
- 던전 콘텐츠: 등급과 번호로 구분하며 고유 서사는 후속 작업
- 저장·복원, 서버 연동, 로그인: 범위 밖
- 등급별 파티 규모 `C·B 3명 / A 4명 / S 5명`: 후속 실험 후보
```

바꿀 블록:

```markdown
- 던전 15개: 테마 3종을 5개씩 나누고 초기 위험도는 ★1 3개 · ★2 4개 · ★3 4개 · ★4 3개 · ★5 1개
- 던전 콘텐츠: 테마 3종(거미굴 · 폐광 · 묘지)이 각각 생태 규칙과 몬스터, 보스를 가진다
- 초기 인력: 캐릭터 30명. 5직업 × 6명이며 캠페인 도중 충원하지 않는다
- 출전 파티: 캐릭터 풀에서 서로 다른 직업 3인을 원정마다 임시 편성
- 길잡이 등급: C·B·A·S를 유지하며 진입할 수 있는 던전 위험도를 정한다
- 저장·복원, 서버 연동, 로그인: 범위 밖
- 자동 전투 장면 연출과 애니메이션: 범위 밖
- 위험도별 출전 규모 확대: 후속 실험 후보
```

- [ ] **Step 9: 설계 판단 체크리스트에 항목을 더한다**

찾을 줄:

```markdown
- 선택 전 위험과 선택 후 원인이 전달되는가?
```

바꿀 줄:

```markdown
- 선택 전 위험과 선택 후 원인이 전달되는가?
- 플레이어가 추론할 근거를 실제로 주는가?
```

- [ ] **Step 10: 테스트를 돌려 이 문서가 통과하는지 확인한다**

Run: `npx vitest run docs/DOCUMENT_TERMINOLOGY`

Expected: FAIL. 단, 위반 목록에서 `GAME_PRINCIPLES.md` 줄이 **모두 사라지고** `design/` 두 문서의 14건만 남는다. 앵커 테스트도 `GAME_PRINCIPLES.md` 줄이 없어야 한다.

`GAME_PRINCIPLES.md` 줄이 남아 있으면 그 용어를 문서에서 직접 찾아 고친다.

```bash
grep -n "완성 파티\|예비 인원\|지속 파티\|자동 재편\|던전 등급\|C 6개" docs/GAME_PRINCIPLES.md
```

- [ ] **Step 11: 커밋**

```bash
git add docs/GAME_PRINCIPLES.md
git commit -m "문서: 게임 원칙을 캐릭터 풀과 위험도 체계로 개정한다"
```

---

## Task 3: GAME_OVERVIEW.md 개정

**Files:**
- Modify: `docs/design/GAME_OVERVIEW.md`
- Test: `docs/DOCUMENT_TERMINOLOGY.test.ts`

**Interfaces:**
- Consumes: Task 2가 확정한 프로토타입 고정 범위의 표현
- Produces: 앵커 `캐릭터 30명` · `위험도`가 들어간 게임 개요

- [ ] **Step 1: 캠페인 목표를 통째로 바꾼다**

찾을 블록:

```markdown
캠페인은 던전 15개와 완성 파티 15팀, 예비 인원 6명으로 시작한다. 초기 던전 분포는 C 6개, B 4개, A 3개, S 2개다.

파티원 한 명이라도 살아서 보스전을 끝내면 던전은 클리어되어 사라진다. 전멸하면 던전은 남고 한 등급 위험해진다. 모든 던전을 없애면 캠페인을 정상 완주하고, 영구적으로 달성한 C·B·A·S 길잡이 등급을 최종 평가로 받는다.
```

바꿀 블록:

```markdown
캠페인은 던전 15개와 캐릭터 30명으로 시작한다. 던전은 테마 3종을 5개씩 나눠 가지며 초기 위험도가 ★1부터 ★5까지 흩어져 있다. 캐릭터는 5직업 6명씩이고 캠페인 도중 충원되지 않는다.

원정마다 캐릭터 풀에서 서로 다른 직업 3인을 임시로 편성한다. 한 명이라도 살아서 보스전을 끝내면 던전은 클리어되어 사라진다. 전멸하면 던전은 남고 위험도가 1 오른다. 모든 던전을 없애면 캠페인을 정상 완주하고, 영구적으로 달성한 C·B·A·S 길잡이 등급을 최종 평가로 받는다.
```

- [ ] **Step 2: `누구를 살릴 것인가`를 바꾼다**

찾을 문장:

```markdown
파티를 살리면 명성, 계약금, 관계와 다음 탐험에 쓸 인력을 보존한다. 일부만 살아도 던전은 클리어되지만 보상은 줄고 파티를 충원하거나 재편해야 한다.
```

바꿀 문장:

```markdown
파티를 살리면 명성, 계약금, 관계와 다음 원정에 쓸 인력을 보존한다. 일부만 살아도 던전은 클리어되지만 보상은 줄고 캐릭터 풀이 그만큼 줄어든다.
```

- [ ] **Step 3: `누구를 희생할 것인가`를 바꾼다**

찾을 문장:

```markdown
전멸한 파티의 소지 골드는 길잡이가 몰래 챙긴다. 대신 명성을 잃고 파티 자원이 사라지며 던전이 더 위험해진다. 의도적 전멸도 유효한 전략이지만 장기 대가가 분명해야 한다.
```

바꿀 문장:

```markdown
전멸한 파티의 소지 골드는 길잡이가 몰래 챙긴다. 대신 명성을 잃고 캐릭터 세 명이 영영 사라지며 던전의 위험도가 오른다. 의도적 전멸도 유효한 전략이지만 장기 대가가 분명해야 한다.
```

- [ ] **Step 4: `무엇을 믿게 할 것인가`에 비공개 조항을 더한다**

찾을 문장:

```markdown
정보 카드는 용사 파티에게만 전달한다. 같은 카드도 파티원마다 수용·의심·적발 결과가 다르다. 보스 관련 정보는 보스전 피해와 생존에 직접 연결된다.
```

바꿀 문장:

```markdown
정보 카드는 용사 파티에게만 전달한다. 같은 카드도 파티원마다 수용·의심·적발 결과가 다르다. 보스 관련 정보는 보스전 피해와 생존에 직접 연결된다.

카드의 진위는 선택 전에 보이지 않는다. 던전의 생태 규칙과 현장 단서를 읽어야 무엇이 거짓인지 가늠할 수 있다.
```

- [ ] **Step 5: `무엇을 쌓을 것인가`를 통째로 바꾼다**

찾을 문장:

```markdown
현재 명성은 공고 지원과 승급 점수에, 누적 획득 골드는 승급 점수에 사용한다. 현재 골드는 상인과 사건에서 소비한다. 명성과 골드 어느 쪽에 집중해도 승급할 수 있지만 낮은 명성은 공고 선택을 막는다.
```

바꿀 문장:

```markdown
명성이 요구치에 닿으면 무료로 승급하고, 닿지 않아도 골드를 지불해 승급할 수 있다. 승급은 자동으로 일어나지 않으며 플레이어가 승급 화면에서 직접 선택한다. 길잡이 등급이 오르면 더 위험한 던전에 진입할 수 있으므로 승급은 곧 콘텐츠 해금이다. 현재 골드는 상인과 사건에서도 소비하므로, 골드를 원정에 쓸지 승급에 쓸지가 결정이 된다.
```

- [ ] **Step 6: `플레이어가 만드는 이야기`의 마지막 항목을 바꾼다**

명성 하한이 0이 되어 명성으로 자격을 잃는 결말이 사라졌다.

찾을 줄:

```markdown
- 큰돈을 얻었지만 명성이 무너져 자격을 잃은 고블린
```

바꿀 줄:

```markdown
- 큰돈을 얻었지만 너무 많은 사람에게 정체를 들켜 고발당한 고블린
```

- [ ] **Step 7: 조기 엔딩 목록을 바꾼다**

찾을 블록:

```markdown
- 생존한 출전 파티원 전원의 신뢰가 0이 되어 처형당함
- 현재 게시판의 모든 공고가 명성 제한에 막힘
- 더 이상 완성 가능한 파티가 없음
```

바꿀 블록:

```markdown
- 원정 중 생존한 파티원 전원의 신뢰가 0이 되어 처형당함
- 신뢰가 0이 된 캐릭터가 5명 누적되어 길드에 고발당함
- 진입할 수 있는 던전이 남지 않아 일감을 잃음
- 서로 다른 직업 3인을 편성할 수 없어 인력이 소진됨
```

- [ ] **Step 8: `프로토타입과 후속 확장`을 바꾼다**

찾을 문장:

```markdown
프로토타입은 모든 등급에서 3인 파티를 사용하고 던전을 등급과 번호로만 구분한다. A급 4인·S급 5인 파티, 고유 던전 테마, 저장과 복원은 첫 캠페인 백테스트 이후 별도 설계한다.
```

바꿀 문장:

```markdown
프로토타입은 모든 위험도에서 3인 파티를 사용하고 테마 3종으로 던전을 구분한다. 출전 규모 확대, 자동 전투 장면 연출, 저장과 복원은 별도 설계한다.
```

- [ ] **Step 9: 테스트를 돌려 이 문서가 통과하는지 확인한다**

Run: `npx vitest run docs/DOCUMENT_TERMINOLOGY`

Expected: FAIL. 위반 목록에 `design/CORE_GAME_LOOP.md`의 10건만 남는다. `GAME_PRINCIPLES.md`와 `design/GAME_OVERVIEW.md` 줄은 없어야 한다.

- [ ] **Step 10: 커밋**

```bash
git add docs/design/GAME_OVERVIEW.md
git commit -m "문서: 게임 개요를 캐릭터 풀과 엔딩 5종으로 개정한다"
```

---

## Task 4: CORE_GAME_LOOP.md 개정

**Files:**
- Modify: `docs/design/CORE_GAME_LOOP.md`
- Test: `docs/DOCUMENT_TERMINOLOGY.test.ts`

**Interfaces:**
- Consumes: Task 2·3이 확정한 캐릭터 풀과 위험도 표현
- Produces: 앵커 `월드턴` · `위험도`가 들어간 루프 문서. 단계 식별자 `intro`와 `worldTurn`은 `F1` 도메인 계약과 `I1` 상태 머신이 그대로 쓴다

- [ ] **Step 1: 캠페인 루프 블록을 바꾼다**

찾을 블록 (```text 펜스 안):

```text
캠페인 생성
→ 공고 게시판
→ 계약과 파티 선택
→ 던전 탐험
→ 보스전
→ 정산·승급·파티 유지
→ 다음 게시판 또는 캠페인 엔딩
```

바꿀 블록:

```text
캠페인 생성
→ 인트로
→ 공고 게시판
→ 계약과 임시 파티 편성
→ 던전 탐험
→ 보스전
→ 정산·위험도·승급
→ 월드턴
→ 다음 게시판 또는 캠페인 엔딩
```

이어지는 문단도 바꾼다. 찾을 문장:

```markdown
캠페인은 던전 15개를 모두 클리어하거나 조기 엔딩이 발생할 때까지 이 흐름을 반복한다. 한 탐험의 HP·신뢰·사망·자원·던전 결과는 다음 탐험에도 남는다.
```

바꿀 문장:

```markdown
캠페인은 던전 15개를 모두 클리어하거나 조기 엔딩이 발생할 때까지 이 흐름을 반복한다. 한 원정의 HP·신뢰·사망·자원·던전 결과는 다음 원정에도 남는다.
```

- [ ] **Step 2: `1. 캠페인 생성`을 통째로 바꾼다**

찾을 블록:

```markdown
캠페인 시드로 다음을 한 번에 확정한다.

- C 6개, B 4개, A 3개, S 2개의 던전과 같은 등급 안의 정렬 순서
- 서로 다른 직업으로 구성된 3인 파티 15팀
- 직업·성격·초기 신뢰가 정해진 예비 인원 6명
- 파티원별 20~45의 소지 골드
- C급 길잡이, 현재 명성 0, 현재 골드 10, 누적 획득 골드 0

시작 골드 10은 누적 획득 골드에 포함하지 않는다.
```

바꿀 블록:

```markdown
캠페인 시드로 다음을 한 번에 확정한다.

- 테마 3종을 5개씩 나눈 던전 15개와 초기 위험도 ★1 3개 · ★2 4개 · ★3 4개 · ★4 3개 · ★5 1개
- 던전마다 초기 위험도로 정해지는 일반 지점 수와 활성 생태 규칙 3개
- 5직업 × 6명, 5성격 × 6명의 캐릭터 30명
- 캐릭터별 20~45의 소지 골드와 성격별 초기 신뢰
- C급 길잡이, 현재 명성 30, 현재 골드 10, 누적 획득 골드 0

시작 골드 10은 누적 획득 골드에 포함하지 않는다. 캐릭터는 캠페인 도중 충원되지 않으므로 사망과 신뢰 0은 되돌릴 수 없다.
```

- [ ] **Step 3: `2. 공고 게시판`을 통째로 바꾼다**

찾을 블록:

```markdown
남은 던전을 C→B→A→S 순으로 정렬하고 같은 등급은 캠페인 시드 순서를 유지한다. 앞에서부터 최대 5개 던전을 출전 가능한 완성 파티와 시드로 짝짓는다.

- 완성 파티가 1~4팀이면 그 수만큼만 공고를 만든다.
- 던전과 파티는 영구 1:1 관계가 아니다.
- 같은 던전은 실패 후 다른 파티와 연결될 수 있다.
- 방금 탐험한 파티도 다음 게시판에 즉시 등장할 수 있다.
- 명성이 부족한 공고는 보이지만 선택할 수 없다.
```

바꿀 블록:

```markdown
길잡이 등급으로 진입할 수 있는 위험도의 미클리어 던전을 위험도 높은 순으로 최대 5개까지 게시한다. 공고를 만들 때마다 출전 가능한 캐릭터에서 서로 다른 직업 3인을 시드로 편성한다.

- 진입 가능한 던전이 5개보다 적으면 남는 자리에 진입 불가 던전을 사유와 함께 보여준다.
- 던전과 캐릭터는 영구 1:1 관계가 아니다.
- 같은 던전은 실패 후 다른 캐릭터들과 연결될 수 있다.
- 방금 원정을 마친 캐릭터도 출전 가능하면 다음 게시판에 다시 나올 수 있다.
- 진입 불가 공고는 숨기지 않고 사유를 보여주되 선택할 수 없다.
- 진입 가능한 던전이 하나도 없으면 막힌 공고를 보여준 뒤 실직 엔딩으로 넘어간다.
```

- [ ] **Step 4: `3. 계약과 파티 확인`을 바꾼다**

찾을 문장:

```markdown
계약 전에 던전 등급, 지원 최소 명성, 계약 보상, 전체 경로와 위험 성격, 파티원별 직업·성격·HP·신뢰·소지 골드를 확인한다.
```

바꿀 문장:

```markdown
계약 전에 던전 이름과 위험도, 진입 조건, 계약 보상, 공개 위험 태그, 답사로 알아낸 생태 규칙, 전체 경로 구조, 출전 캐릭터별 직업·성격·HP·신뢰·소지 골드를 확인한다.
```

- [ ] **Step 5: `4. 던전 탐험`의 정보 기회 문단을 바꾼다**

찾을 문장:

```markdown
카드는 모든 지점에서 나오지 않는다. 실제 선택 경로의 정보 전달 기회 횟수는 C 2회, B 3회, A 4회, S 5회다. 사건 행동은 카드와 별도로 선택한다.
```

바꿀 문장:

```markdown
카드는 모든 지점에서 나오지 않는다. 실제 선택 경로의 정보 전달 기회는 현재 위험도를 따라 ★1 2회 · ★2 3회 · ★3 4회 · ★4 5회 · ★5 5회다. 카드의 진위는 선택 전에 보이지 않으며, 플레이어는 공개된 생태 규칙과 현장 단서로 판단한다. 사건 행동은 카드와 별도로 선택한다.
```

- [ ] **Step 6: `5. 보스전`에 턴 기록을 더한다**

찾을 문장:

```markdown
파티원 한 명 이상 생존하면 던전 클리어, 전멸하면 실패다.
```

바꿀 문장:

```markdown
전투는 턴 단위로 진행하며 각 턴의 행동과 피해를 기록으로 남긴다. 플레이어는 공격이나 방어를 직접 조작하지 않는다. 앞선 판단과 누적된 상태가 결과로 드러난다.

파티원 한 명 이상 생존하면 던전 클리어, 전멸하면 실패다.
```

- [ ] **Step 7: `6. 정산과 다음 캠페인 상태`의 순서를 바꾼다**

찾을 블록:

```markdown
1. 생존자, 신뢰, 계약 보상 또는 전멸 손실과 유품 계산
2. 던전 제거 또는 위험 등급 상승
3. 승급 점수 계산과 영구 등급 갱신
4. 예비 인원 충원 또는 생존자 자동 재편
5. 출전하지 않은 모든 생존자의 HP 회복
6. 엔딩 판정
7. 계속되면 다음 게시판 생성
```

바꿀 블록:

```markdown
1. 생존자, 신뢰, 계약 보상 또는 전멸 손실과 유품 계산
2. 클리어한 던전 제거 또는 실패한 던전의 위험도 1 상승
3. 승급 가능 여부 갱신. 승급 자체는 플레이어가 승급 화면에서 직접 선택한다
4. 월드턴 처리. 출전하지 않은 캐릭터를 휴식과 백그라운드 원정에 배정한다
5. 엔딩 판정
6. 계속되면 다음 게시판 생성
```

- [ ] **Step 8: 진행 단계 표를 바꾼다**

찾을 블록:

```markdown
| 단계 | 식별자 | 역할 |
| --- | --- | --- |
| 공고 게시판 | `campaignBoard` | 던전·파티 공고 비교와 계약 선택 |
| 파티 확인 | `partyIntro` | 출전 파티의 개인 상태 확인 |
| 경로 선택 | `pathChoice` | 계약 직후 공개된 전체 지도에서 다음 지점 선택 |
| 정보 전달 | `infoOpportunity` | 일부 지점에서 진실·거짓·중립 카드 선택 |
| 사건 | `event` | 사건별 행동 선택과 결과 처리 |
| 보스전 | `bossFight` | 누적 상태로 자동 전투와 정보 검증 |
| 정산 | `settlement` | 보상·손실·파티·던전·승급 갱신 |
| 캠페인 종료 | `campaignEnded` | 정상 완주 등급 또는 조기 엔딩 표시 |
```

바꿀 블록:

```markdown
| 단계 | 식별자 | 역할 |
| --- | --- | --- |
| 인트로 | `intro` | 길잡이 역할과 캠페인 목표 안내 |
| 공고 게시판 | `campaignBoard` | 던전 공고 비교와 계약 선택 |
| 파티 확인 | `partyIntro` | 편성된 출전 캐릭터의 개인 상태 확인 |
| 경로 선택 | `pathChoice` | 계약 직후 공개된 전체 지도에서 다음 지점 선택 |
| 정보 전달 | `infoOpportunity` | 일부 지점에서 카드 세 장 중 하나를 선택 |
| 사건 | `event` | 사건별 행동 선택과 결과 처리 |
| 보스전 | `bossFight` | 누적 상태로 턴 단위 자동 전투와 정보 검증 |
| 정산 | `settlement` | 보상·손실·던전 위험도·승급 가능 여부 갱신 |
| 월드턴 | `worldTurn` | 비출전 캐릭터의 휴식과 백그라운드 원정 처리 |
| 캠페인 종료 | `campaignEnded` | 정상 완주 등급 또는 조기 엔딩 표시 |
```

이어지는 문장도 바꾼다. 찾을 문장:

```markdown
규칙에 없는 단계 전이는 거부한다. 전멸로 탐험이 중간 종료되면 남은 사건과 보스전을 건너뛰고 정산으로 간다.
```

바꿀 문장:

```markdown
규칙에 없는 단계 전이는 거부한다. 전멸로 원정이 중간 종료되면 남은 사건과 보스전을 건너뛰고 정산으로 간다. `intro`는 캠페인당 한 번만 지나간다.
```

- [ ] **Step 9: 캠페인 자원 표를 바꾼다**

찾을 블록:

```markdown
| 자원 | 의미 |
| --- | --- |
| 현재 명성 | 공고 지원과 승급 점수에 사용하며 음수가 될 수 있음 |
| 현재 골드 | 상인·사건에서 소비하며 0 미만이 될 수 없음 |
| 누적 획득 골드 | 모든 실제 골드 수입을 더하며 소비해도 줄지 않음 |
| 승급 점수 | `현재 명성 × 2 + 누적 획득 골드` |
```

바꿀 블록:

```markdown
| 자원 | 의미 |
| --- | --- |
| 현재 명성 | 무료 승급 조건에 사용하며 0 미만이 될 수 없음 |
| 현재 골드 | 상인·사건 소비와 골드 승급에 사용하며 0 미만이 될 수 없음 |
| 누적 획득 골드 | 모든 실제 골드 수입을 더하며 엔딩 회고에만 사용 |

던전 진입 자격은 명성이 아니라 길잡이 등급이 정한다. 명성은 그 등급을 올리는 데 쓴다.
```

- [ ] **Step 10: 결과 피드백의 마지막 항목을 바꾼다**

찾을 줄:

```markdown
- 캠페인: 승급, 파티 충원·재편·회복, 다음 공고, 엔딩 위험
```

바꿀 줄:

```markdown
- 캠페인: 승급 가능 여부, 월드턴의 회복과 백그라운드 결과, 다음 공고, 엔딩 위험
```

- [ ] **Step 11: 테스트를 돌려 전부 통과하는지 확인한다**

Run: `npx vitest run docs/DOCUMENT_TERMINOLOGY`

Expected: PASS. 3개 테스트 모두 통과한다.

남는 위반이 있으면 해당 문서에서 직접 찾는다.

```bash
grep -n "완성 파티\|예비 인원\|자동 재편\|승급 점수\|던전 등급\|C 6개\|C→B→A→S\|현재 명성 0\|음수가 될 수 있음\|C 2회" docs/design/CORE_GAME_LOOP.md
```

- [ ] **Step 12: 커밋**

```bash
git add docs/design/CORE_GAME_LOOP.md
git commit -m "문서: 핵심 게임 루프에 인트로와 월드턴 단계를 넣는다"
```

---

## Task 5: 배정표 갱신과 전체 검증

**Files:**
- Modify: `docs/technical/CAMPAIGN_REWORK_WORK_ASSIGNMENT.md`

**Interfaces:**
- Consumes: Task 2~4의 완료
- Produces: `D2`~`D5`가 시작 가능한 상태의 배정표

- [ ] **Step 1: D1 행의 상태를 완료로 바꾼다**

`| D1 | 최상위·루프 문서 개정 | ... | — | **D2 D3 D4 D5** | LatteBun | ⬜ |` 행에서 마지막 칸 `⬜`를 `✅`로 바꾼다.

- [ ] **Step 2: D2~D5 행의 선행에서 D1을 지운다**

네 행의 `선행` 칸이 모두 `D1`이다. 각각 `—`로 바꾼다. 배정표의 관리 원칙이 `완료된 ID는 다른 행의 선행에서 지운다`이며 무결성 검사가 이를 강제한다.

- [ ] **Step 3: 시작 가능한 작업 문단을 갱신한다**

찾을 문장:

```markdown
착수 시점에 선행이 없는 것은 `D1` 하나다. 의도한 구조다. `D1`이 끝나면 `D2`·`D3`·`D4`·`D5` 네 개가 동시에 열려 세 명이 병렬로 붙을 수 있다.
```

바꿀 문장:

```markdown
`D1`이 끝나 `D2`·`D3`·`D4`·`D5` 네 개가 열려 있다. 세 명이 동시에 붙을 수 있다.
```

- [ ] **Step 4: 무결성 검사를 돌린다**

Run: `npx vitest run docs/technical`

Expected: PASS. 30개 테스트 통과. 실패하면 메시지가 어느 행의 어느 칸이 어긋났는지 짚어 준다.

- [ ] **Step 5: 전체 검증을 돌린다**

```bash
npx vitest run
npx tsc --noEmit
npx eslint .
```

Expected: 테스트 46파일 전부 통과(`DOCUMENT_TERMINOLOGY.test.ts`가 늘어 45 → 46), 타입 오류 없음, lint 오류 없음.

- [ ] **Step 6: 문서 간 모순이 없는지 눈으로 확인한다**

감시 테스트는 용어만 본다. 문장의 앞뒤가 맞는지는 사람이 본다. 세 문서를 이어서 읽고 아래를 확인한다.

- `GAME_PRINCIPLES.md`의 프로토타입 고정 범위와 `CORE_GAME_LOOP.md`의 캠페인 생성이 같은 수치를 말하는가
- `GAME_OVERVIEW.md`의 조기 엔딩 4종이 `CORE_GAME_LOOP.md`의 정산 순서와 어긋나지 않는가
- 세 문서 어디에도 확정되지 않은 수치가 사실처럼 적혀 있지 않은가

`docs/systems/`와 `docs/experience/`는 아직 옛 규칙을 담고 있다. 이번 작업의 범위가 아니며 `D2`~`D5`가 맡는다. 지금 고치지 않는다.

- [ ] **Step 7: 커밋**

```bash
git add docs/technical/CAMPAIGN_REWORK_WORK_ASSIGNMENT.md
git commit -m "배정표: D1을 완료로 표시하고 D2~D5를 연다"
```

---

## 완료 기준

- `npx vitest run docs/DOCUMENT_TERMINOLOGY` 3개 테스트 통과
- `npx vitest run docs/technical` 30개 테스트 통과
- `npx vitest run` · `npx tsc --noEmit` · `npx eslint .` 모두 통과
- 세 문서에서 `완성 파티` · `예비 인원` · `지속 파티` · `승급 점수` · `던전 등급`이 0건
- 세 문서가 캐릭터 풀 30명, 위험도 ★1~5, 테마 3종을 프로토타입 범위로 명시
- 배정표에서 `D1`이 `✅`이고 `D2`~`D5`의 선행이 `—`
