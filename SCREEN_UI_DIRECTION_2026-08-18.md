# Dungeon Schemer — 화면 UI 방향 정리

> 날짜: 2026-08-18  
> 상태: 화면 구조 논의 정리 / UI 시안 기준  
> 관련 기준: 2026-08-17 회의 정리, 현재 개발 환경(Next.js + React + TypeScript + Tailwind CSS + Framer Motion + Zustand)

---

# 1. 전체 화면 공통 원칙

## 1-1. 고정 화면 구조

주요 플레이 화면은 가능한 한 같은 전체 프레임과 비율을 유지한다.

기본 방향은 다음과 같다.

```text
세계/게임 화면 : 플레이어 정보 영역
       3       :        1
```

대략적으로:

- 왼쪽 약 75%: 게임 세계, 게시판, 지도, 자동 진행, 플레이어 조작 영역
- 오른쪽 약 25%: 파티 상태, 선택 상세, 결과 정보 등

화면마다 전체 구조를 크게 갈아엎지 않고, 같은 게임 셸 안에서 콘텐츠만 전환한다.

## 1-2. 공통 상단 정보 바

게시판, 지도, 진행 화면에서 공통적으로 상단 상태 바를 유지한다.

표시 후보:

- 길잡이 등급
- 현재 명성
- 골드
- 승급 진행
- 남은 던전 수

던전 화면에서는 필요하면 던전 이름과 위험도를 함께 표시한다.

## 1-3. 시각 분위기

전체 UI는 다음 분위기로 통일한다.

- 다크 판타지
- 짙은 갈색 / 검은색 배경
- 낡은 목재 프레임
- 금속 장식
- 양피지 질감
- 촛불과 따뜻한 광원
- 금색 계열 테두리와 텍스트 포인트
- 초록색: 활성, 선택, 진행 가능
- 붉은색: 위험, 지원 불가, 보스, 경고

단순 관리 프로그램처럼 보이기보다 실제 길드와 던전 세계 안에 존재하는 UI처럼 보이게 한다.

## 1-4. 하이브리드 UI 원칙

세계관 비주얼과 실제 조작 UI를 분리하지 않고 결합한다.

예:

- 게시판 배경은 실제 길드 게시판처럼 표현
- 공고 종이는 실제 클릭 가능한 UI 카드로 구현
- 던전 지도는 실제 공간처럼 보이게 표현하되 경로 선택 상태는 UI로 명확하게 표시
- 던전 진행 연출은 실제 장면으로 보여주되 하단 선택 영역은 읽기 쉬운 카드 UI로 유지

즉, "일러스트처럼 보이지만 실제로 조작 가능한 UI"를 목표로 한다.

---

# 2. 캠페인 게시판 화면

> **참고용 이미지** — 아래 이미지는 최종 구현을 고정하는 산출물이 아니라, 게시판 화면의 레이아웃·분위기·정보 배치 방향을 공유하기 위한 참고용 시안이다.
> [게시판 화면 참고 이미지 열기](docs\diagram\png\REFERENCE_UI_01_CAMPAIGN_BOARD.png)

![게시판 화면 참고용 시안](docs\diagram\png\REFERENCE_UI_01_CAMPAIGN_BOARD.png)


## 2-1. 기본 구조

```text
┌────────────────────────────────────┬──────────────┐
│                                    │              │
│          길드 게시판 / 공고          │  선택 상세    │
│                                    │              │
│          최대 5개 공고              │  던전 정보    │
│                                    │  파티 정보    │
│                                    │  보상/계약    │
└────────────────────────────────────┴──────────────┘
```

## 2-2. 왼쪽 — 길드 게시판

방향은 **하이브리드형**으로 확정한다.

- 나무 게시판, 핀, 종이, 테이프 등의 세계관 표현 사용
- 공고는 실제 양피지/수배지처럼 보이게 배치
- 단, 각 공고는 별도의 클릭 가능한 UI 카드로 구현
- 공고 최대 5개 표시
- 다섯 장은 지나치게 반듯하게 정렬하지 않고 약간의 위치 차이와 기울기를 둔다.
- 완전히 난잡하게 흩뜨리지는 않고 읽기 쉬운 수준의 비대칭을 유지한다.

### 공고 종이에 표시할 정보

파티 정보는 공고 종이에 넣지 않는다.

표시 항목:

- 던전 이름
- 위험도 ★1~5
- 지원 가능 / 지원 불가
- 보상 요약
- 공개 위험 태그 1~2개

예:

```text
잿빛 기록보관소
위험도 ★★★☆☆
지원 가능
보상 10 명성 / 20 골드
함정 / 정보
```

### 선택 상태

선택된 공고는 다음 방식으로 강조한다.

- 테두리 강조
- 종이 밝기 소폭 상승
- 핀 또는 고정 장식 강조

과도한 확대는 하지 않는다.

### 지원 불가 공고

지원 불가 공고도 게시판에서 숨기지 않는다.

표현:

- 붉은 테두리
- 지원 불가 문구

공고 자체는 클릭 가능하게 두고, 오른쪽 상세 패널에서 지원 불가 사유를 확인할 수 있게 한다.

예:

```text
지원 불가
필요 명성 60
현재 명성 38
```

## 2-3. 오른쪽 — 선택 상세

선택한 공고의 상세 내용을 보여준다.

### 던전 정보

- 던전 이름
- 위험도
- 공개 위험 태그
- 전체 지점 수
- 분기 구조
- 짧은 던전 설명

### 출전 파티 정보

파티원 3명을 각각 카드 형태로 표시한다.

각 카드:

- 초상화
- 이름
- 직업
- 성격
- HP
- 개인 신뢰
- 소지 골드

파티 이름도 별도로 표시할 수 있다.

### 보상 / 계약

하단에 계약 영역을 둔다.

- 획득 명성
- 획득 골드
- 지원 조건
- 지원 가능 여부
- 계약 버튼

계약 버튼은 시각적으로 명확한 주요 액션으로 표현한다.

---

# 3. 던전 지도 화면

> **참고용 이미지** — 아래 이미지는 최종 구현을 고정하는 산출물이 아니라, 실제 던전 공간처럼 보이는 지도 표현과 우측 파티 상태 패널의 방향을 공유하기 위한 참고용 시안이다.
> [던전 지도 화면 참고 이미지 열기](docs\diagram\png\REFERENCE_UI_02_DUNGEON_MAP.png)

![던전 지도 화면 참고용 시안](docs\diagram\png\REFERENCE_UI_02_DUNGEON_MAP.png)


## 3-1. 기본 목표

예전의 원형 노드 그래프 와이어프레임처럼 보이지 않게 한다.

실제로 던전 안의 구조를 들여다보는 것 같은 **공간형 지도**로 표현한다.

## 3-2. 화면 구조

```text
┌────────────────────────────────────┬──────────────┐
│                                    │              │
│          실제 공간형 던전 지도       │  파티 상태    │
│                                    │              │
│          경로/방/분기/합류            │              │
│                                    │              │
│                                    │  이동 버튼    │
└────────────────────────────────────┴──────────────┘
```

## 3-3. 범례

별도의 범례 영역은 **완전히 삭제**한다.

지도 자체만 보고 다음 상태를 이해할 수 있어야 한다.

- 현재 위치
- 방문 완료
- 선택 가능한 다음 지점
- 현재 선택할 수 없는 다른 갈래
- 보스 위치

## 3-4. 지도 표현

단순한 선 + 원형 노드가 아니라 실제 던전의 다음 요소를 시각화한다.

- 방
- 복도
- 갈림길
- 합류 지점
- 폐쇄된 공간
- 보스방

지도는 완전히 사실적인 탑다운 게임 화면이 아니라, 실제 공간과 UI 지도를 섞은 형태로 표현한다.

### 경로 구조

- 한 지점에서 다음 선택지는 최대 2개
- 분기된 경로는 뒤에서 다시 합쳐질 수 있음
- 보스 위치는 처음부터 공개
- 전체 연결 구조도 공개
- 정확한 피해, 보상, 난수 결과는 숨김

### 상태 표현

- 현재 위치: 초록색 강조
- 방문 완료: 약한 초록/회색
- 선택 가능: 금색 또는 밝은 강조
- 비활성/다른 갈래: 어둡게 처리하거나 점선 느낌
- 보스방: 강한 붉은색 문양, 봉인, 마법진 등

## 3-5. 던전 표기 방식

던전의 C/B/A/S 등급은 사용하지 않는다.

표시는 다음처럼 통일한다.

```text
잿빛 기록보관소
위험도 ★★★☆☆
```

길잡이 C/B/A/S 등급과 던전 위험도는 서로 다른 개념으로 분리한다.

## 3-6. 오른쪽 — 파티 상태

파티원 3명을 카드 형태로 표시한다.

각 카드:

- 초상화
- 이름 / 직업
- HP
- 신뢰
- 소지 골드
- 현재 상태 또는 최근 기록

하단에는 현재 선택한 다음 지점과 이동 버튼을 둔다.

예:

```text
다음 지점: 정보
[선택 지점 입장]
```

---

# 4. 던전 진행 / 정보 선택 화면

> **참고용 이미지** — 아래 이미지는 최종 구현을 고정하는 산출물이 아니라, 자동 전투 연출 약 40% / 플레이어 조작 약 60% 구조와 전체 다크 판타지 UI 분위기를 공유하기 위한 참고용 시안이다.
> [던전 진행 화면 참고 이미지 열기](docs\diagram\png\REFERENCE_UI_03_DUNGEON_PROGRESS.png)

![던전 진행 화면 참고용 시안](docs\diagram\png\REFERENCE_UI_03_DUNGEON_PROGRESS.png)


## 4-1. 역할

이 화면은 던전 플레이의 핵심 화면이다.

플레이어가 직접 전투하는 것이 아니라:

1. 용사 파티가 자동으로 행동하는 모습을 보고
2. 현재 상황과 단서를 읽고
3. 카드 또는 행동을 선택하여 개입하고
4. 그 결과가 다시 자동 연출과 파티 상태에 반영되는 구조다.

## 4-2. 전체 구조

전체 3:1 화면 구조는 유지한다.

왼쪽 75%를 다시 위/아래로 나눈다.

```text
┌────────────────────────────────────┬──────────────┐
│                                    │              │
│         자동 진행 / 자동 전투        │              │
│             약 40%                 │  파티 상태    │
│                                    │              │
├────────────────────────────────────┤              │
│                                    │              │
│       상황 설명 + 선택 카드 3장       │              │
│             약 60%                 │              │
│                                    │              │
└────────────────────────────────────┴──────────────┘
```

현재 기준 추천 비율:

```text
자동 연출 : 플레이어 조작
    4     :      6
```

실제 구현 과정에서 카드 텍스트와 상황 설명이 더 많은 공간을 필요로 하면 3.5:6.5 또는 3:7까지 조정할 수 있다.

플레이어 조작 영역을 자동 연출보다 넓게 두는 것을 기본 원칙으로 한다.

## 4-3. 좌측 상단 — 자동 진행 / 자동 전투 연출

이 영역은 단순 정보 패널이 아니라 **실제 던전 장면**을 보여준다.

표현 대상:

- 용사 3명이 이동하는 모습
- 몬스터 등장
- 자동 공격
- 피격
- 함정 발동
- 휴식
- 상인 조우
- 사건 발생
- 짧은 대사 또는 감정 반응
- HP 변화
- 보스전

플레이어는 이 영역에서 직접 공격/방어를 조작하지 않는다.

전투는 앞선 선택과 현재 상태가 결과로 드러나는 자동 연출이다.

### 연출 목적

자동 전투는 게임의 주 조작 영역이 아니다.

따라서 화면 대부분을 차지하지 않고, 다음을 이해할 수 있을 정도로만 충분히 보여준다.

- 지금 어떤 사건이 벌어지고 있는가
- 누구에게 피해가 발생했는가
- 플레이어의 과거 선택이 어떤 결과를 만들었는가

## 4-4. 좌측 하단 — 플레이어 조작 영역

이 영역이 실제 플레이의 중심이다.

### 상황 설명

현재 사건을 짧고 명확하게 설명한다.

예:

```text
낡은 서가가 심하게 흔들리며 무너져 내리고 있다.
용사들은 어느 통로로 빠져나갈지 판단하지 못하고 있다.
시간이 지나면 더 많은 서가가 붕괴할 것 같다.
```

상황 설명에는 플레이어가 판단에 사용할 수 있는 관찰 정보와 단서를 포함할 수 있다.

### 정보 카드 3장

항상 같은 디자인의 카드 3장을 보여준다.

내부적으로는:

- 진실 1장
- 거짓 1장
- 중립 1장

으로 구성한다.

하지만 **선택 전에는 카드의 유형을 절대로 표시하지 않는다.**

삭제해야 하는 표현:

- 진실 카드
- 거짓 카드
- 중립 카드
- 정답 색상 표시
- 발각 여부
- 예상 신뢰 변화량

세 카드 모두 그럴듯하게 보이게 만든다.

예:

```text
[왼쪽 통로로 빠져나가!]
[중앙 통로가 가장 안전해!]
[바람이 부는 쪽을 따라가!]
```

플레이어는 앞서 얻은 정보, 환경 단서, 몬스터 생태, 이전 사건 기억 등을 바탕으로 선택한다.

## 4-5. 카드 선택 후 흐름

카드 선택 뒤에는 다음 인과관계를 보여준다.

```text
선택
→ 파티원별 반응
→ 실제 사건 결과
→ HP / 신뢰 / 상태 변화
```

예:

```text
에다 — 수용
니오 — 의심
라사 — 적발
```

그 뒤 자동 연출 영역에서 실제 결과가 발생한다.

## 4-6. 오른쪽 — 실시간 파티 상태

진행 화면에서는 게시판/지도보다 더 실시간성이 강조된다.

파티원별로 표시:

- 초상화
- 이름
- 직업
- HP
- 개인 신뢰
- 소지 골드
- 현재 상태
- 최근 반응

예:

```text
에다 · 전사
HP 10 / 12
신뢰 74 ↑2
소지 골드 18G
최근 반응: 수용
"방금 정보는 도움이 됐다."
```

선택 직후에는 반응 상태를 잠시 강조한다.

- 수용: 초록
- 의심: 금색/주황
- 적발: 붉은색

색만으로 의미를 전달하지 않고 텍스트 또는 아이콘을 함께 사용한다.

---

# 5. 세 화면의 연결감

세 화면은 서로 다른 메뉴처럼 보이기보다 하나의 게임 흐름처럼 이어져야 한다.

```text
길드 게시판
→ 실제 공고를 골라 계약

던전 지도
→ 실제 던전 구조를 보고 다음 경로 선택

던전 진행
→ 용사들이 실제 공간에서 움직이고 싸움
→ 플레이어는 하단에서 정보와 행동으로 개입
```

핵심은 화면이 바뀌어도 다음 요소가 계속 유지되는 것이다.

- 공통 상단 바
- 오른쪽 정보 패널 계열
- 동일한 목재/금속 프레임
- 동일한 색상 의미
- 동일한 타이포그래피와 카드 톤

이를 통해 게시판 → 지도 → 진행이 같은 게임 안에서 자연스럽게 연결되도록 한다.

---

# 6. 구현 시 구조 제안

현재 개발 환경 기준으로 공통 게임 셸을 중심으로 구성한다.

예시:

```text
app/play/layout.tsx
└─ GameShell
   ├─ TopStatusBar
   ├─ MainContent
   └─ RightPanel
```

화면별 구성 예시:

```text
게시판
MainContent
└─ CampaignBoard

RightPanel
└─ ContractDetail
```

```text
지도
MainContent
└─ DungeonMap

RightPanel
└─ PartyStatus
```

```text
던전 진행
MainContent
├─ AdventureScene
└─ PlayerDecisionArea

RightPanel
└─ LivePartyStatus
```

게임 도메인을 아는 컴포넌트는 `components/game/`에 두고, 범용 프레임/패널은 `components/ui/`에 둔다.

Framer Motion은 다음과 같은 결과 피드백에 우선 사용한다.

- 공고 선택
- 지도 경로 활성화
- 파티 이동
- 공격/피격
- 카드 선택
- 수용/의심/적발 반응
- HP/신뢰 변화

모션 자체를 보여주기 위해 정보를 늦추거나 가리지 않는다.

---

# 7. 현재 확정된 UI 방향 요약

| 항목                     | 방향                                   |
| ------------------------ | -------------------------------------- |
| 전체 주요 화면 비율      | 왼쪽 3 : 오른쪽 1                      |
| 상단 상태 바             | 화면 간 공통 유지                      |
| 전체 스타일              | 다크 판타지 / 목재 / 금속 / 양피지     |
| 게시판                   | 실제 게시판 + 클릭 가능한 UI 공고 카드 |
| 게시판 공고 수           | 최대 5개                               |
| 공고의 파티 정보         | 표시하지 않음                          |
| 선택 공고                | 밝기 + 테두리 + 핀 강조                |
| 지원 불가 공고           | 붉은 테두리                            |
| 지원 불가 공고 상세 확인 | 가능                                   |
| 던전 표기                | 이름 + 위험도 ★1~5                     |
| 지도 범례                | 삭제                                   |
| 지도 표현                | 실제 던전 공간형 지도                  |
| 보스 위치                | 공개                                   |
| 지도 분기                | 한 지점 최대 2개 / 추후 합류 가능      |
| 진행 화면 상·하 비율     | 자동 연출 약 40% / 조작 약 60%         |
| 자동 전투                | 실제 장면처럼 보여주되 직접 조작 없음  |
| 플레이어 핵심 영역       | 상황 설명 + 카드 3장                   |
| 카드 타입                | 선택 전 비공개                         |
| 진행 중 오른쪽 패널      | 파티원별 HP/신뢰/골드/최근 반응        |
| 선택 결과 표현           | 선택 → 반응 → 사건 → 수치 변화         |

---

# 8. 아직 추후 정할 수 있는 부분

- 실제 기준 해상도 및 화면 종횡비 고정 방식
- 작은 브라우저 창 / 모바일 환경에서의 대응
- 게시판 공고의 정확한 배치 알고리즘
- 지도 카메라 이동/줌 사용 여부
- 자동 연출의 실제 애니메이션 수준
- 카드 본문 최대 길이와 텍스트 크기
- 파티 상태 카드에서 항상 보여줄 정보와 선택적으로 보여줄 정보
- 진행 화면에서 일반 사건 행동과 정보 카드가 같은 UI를 쓸지 별도 UI를 쓸지
- 보스전에서 하단 조작 영역을 어떤 정보/로그 화면으로 전환할지

